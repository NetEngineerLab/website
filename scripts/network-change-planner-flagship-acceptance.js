#!/usr/bin/env node
"use strict";const fs=require("fs"),path=require("path"),assert=require("node:assert/strict"),E=require("../website/tools/network-change-planner-mop-generator/js/engine.js"),{loadToolRegistry}=require("./tool-registry"),{loadWorkflowRegistry}=require("./workflow-registry");const root=path.resolve(__dirname,".."),id="network-change-planner-mop-generator";const tools=loadToolRegistry(),tool=tools.find(x=>x.id===id);assert(tool&&tool.status==="active");assert.equal(tools.length,22);assert(fs.existsSync(path.join(root,"website/tools",id,"index.html")));assert(fs.existsSync(path.join(root,"website/tools",id,"zh/index.html")));const flow=loadWorkflowRegistry().find(x=>x.id==="network-change-automation");assert(flow&&flow.steps.some(x=>x.toolId===id));assert.equal(Object.keys(E.DEVICES).length,5);console.log("V2.1 flagship Network Change Planner acceptance: PASS (22 tools)");