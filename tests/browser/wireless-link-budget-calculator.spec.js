const {test,expect}=require("@playwright/test");
for(const locale of ["en","zh"]){test(`wireless link invalidation and clearance boundary ${locale}`,async({page})=>{const errors=[];page.on("console",m=>{if(m.type()==="error")errors.push(m.text())});page.on("pageerror",e=>errors.push(e.message));await page.goto(`/tools/wireless-link-budget-calculator/${locale==="zh"?"zh/":""}`,{waitUntil:"networkidle"});await expect(page.locator("#review-and-related")).toContainText(locale==="zh"?"内容复核：2026-09-01":"Last reviewed: 2026-09-01");await expect(page.locator("#rxPower")).not.toHaveText("—");await page.locator("#clearanceM").fill("0");await expect(page.locator("#status")).toHaveText(locale==="zh"?"临界":"Marginal");await expect(page.locator("#warnings")).toContainText(locale==="zh"?"菲涅尔区":"Fresnel zone");await page.locator("#distanceKm").fill("");await expect(page.locator("#rxPower")).toHaveText("—");await expect(page.locator("#copy")).toBeDisabled();await expect(page.locator("#print")).toBeDisabled();expect(errors).toEqual([]);});}
