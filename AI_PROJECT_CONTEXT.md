# NetEngineerLab — AI Project Context

> 本文件是 AI / Codex / 开发 Agent 的最小项目上下文入口。
> 日常开发默认先读取本文件，不得为了“了解项目”无目的扫描全仓库。

## 1. 项目定位

NetEngineerLab 是面向网络工程师、运营商运维、规划、设计、安全管理人员的专业网络工程工具平台。

核心目标不是做普通资讯站，而是让用户通过工具完成：

- 工程计算
- 网络分析
- 风险判断
- 设计规划
- 方案生成
- MOP / 报告 / 台账生成
- 工程决策辅助

产品原则：

**用户价值 > 工具数量；深度工具 > 简单计算器；完整工程工作流 > 单点功能。**

免费基础工具负责搜索流量、拉新和建立信任；高级分析、批量处理、专业模板、报告导出及后续专家服务承担商业化。

## 2. 技术与部署基线

- 前端优先
- 主要部署：Cloudflare
- Mobile First / H5 友好
- 多语言
- SEO + GEO / AI Search 友好
- 结构化数据
- Lighthouse / 性能门禁
- 后期按真实需求逐步加入后端能力

不要为了增加技术复杂度而引入后端。

## 3. 关键目录

优先按实际仓库结构确认，当前主要关注：

- `website/tools/`：工具及工具相关页面
- `website/zh/`：中文站相关页面
- `scripts/`：审计、自动检查和构建辅助脚本
- `docs/`：产品、工程、UI、审计及版本规范
- `package.json`：项目脚本入口（如存在）

若实际目录与本文件不一致，以仓库当前结构为准，并在确认后更新本文件。

## 4. UI / 工具页基准

工具页统一模板优先参考：

`/tools/network-change-planner-mop-generator/zh/`

重点保持统一：

- Header / Navigation
- Breadcrumb
- Hero
- 输入区
- 操作按钮
- Result / Report
- Card / Panel
- Content Section
- FAQ
- Footer
- Mobile layout

禁止重新引入已淘汰的旧 DOM、旧 `.start-btn` 或历史模板。

## 5. 工具开发要求

每个新工具开发前必须回答：

1. 用户是谁？
2. 用户遇到什么真实工程问题？
3. 用户输入什么？
4. 系统计算/判断什么？
5. 最终输出什么？
6. 输出能否直接用于实际工作？
7. 相比 Excel、人工计算、普通在线计算器节省了什么？
8. 能否继续升级为报告、批处理、方案或工作流？

禁止为了凑工具数量开发低价值重复计算器。

## 6. Token 节约模式（默认）

日常开发默认开启。

1. 首先读取本文件。
2. 禁止无必要的全项目扫描。
3. 优先查看本次任务相关文件和 `git diff`。
4. 只读取本次修改文件及直接依赖。
5. 已 PASS 的模块不得无理由重复审计。
6. 批量机械检查优先写/运行脚本，不让 AI 逐页阅读。
7. 只有公共组件、架构、模板、多语言、SEO 等发生跨站影响时才扩大范围。
8. 不因用户只说“继续”而重新扫描整个项目。
9. 修改后先做局部测试和 Diff 审计。
10. 大版本、架构级变更、最终上线验收才允许全量审计。

## 7. Agent 审计分工

- **1号开发**：完成本次任务，只检查修改范围和必要依赖。
- **2号审计**：默认只审本轮 Git Diff、修改文件和直接依赖。
- **3号复验**：只复验 2号 FAIL 和对应修复，不重新全量扫描。
- **4号总工**：优先读取变更摘要、审计结果、测试结果和剩余风险。
- **最终上线验收**：允许全量扫描，但应先运行自动化审计，再对 FAIL 深查。

## 8. SEO / GEO / 多语言底线

涉及页面新增或模板修改时检查必要项目：

- Title / Description
- Canonical
- hreflang
- Sitemap
- robots / llms（涉及全局时）
- Structured Data
- Internal Links
- FAQ（适用时）
- 多语言内容完整性
- 中文页避免无意义英文 UI 泄漏
- Mobile / Accessibility / Performance

不要在每个局部任务中重复全站 SEO 扫描。

## 9. 当前发展方向

优先发展具有工程深度和组合价值的工具，例如：

- 网络设计与容量规划
- 数据中心网络与收敛/Fabric分析
- OLT / 接入网设计
- 传输环优化与风险分析
- 网络变更计划 / MOP
- 电源、能耗、PUE、油机、光伏等通信基础设施工具
- 风险检查、隐患判定、报告生成

工具之间应逐渐形成“输入 → 分析 → 方案 → 报告/导出”的工作流，而不是孤立页面。

## 10. AI 开工规则

用户采用简写：

`NetEngineerLab，节约模式，今天开发【任务】`

AI 应自动理解为：

**读取本文件 → 确定最小任务范围 → 读取必要文件/Diff → 开发 → 局部测试 → 2号 Diff 审计 → 修复 FAIL → 必要时复验。**

除非用户明确说“最终上线全量验收”，否则不要默认进行全项目扫描。
