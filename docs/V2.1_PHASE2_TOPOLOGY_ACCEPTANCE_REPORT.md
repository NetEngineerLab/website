# V2.1 Phase2 Device Topology + LAG Acceptance Report

## Status
ACCEPTED — topology/LAG feature gates passed.

## Verified
- member-aware physical interface resolution
- member out-of-range rejection
- explicit slot + line-card resolution contract
- physical port bounds
- breakout parent/child resolution and lane bounds
- canonical LAG validation
- Cisco IOS Port-channel render/parse round-trip
- Huawei VRP Eth-Trunk render/parse round-trip
- H3C Comware Bridge-Aggregation render/parse round-trip
- Juniper Junos ae render/parse round-trip
- Arista EOS Port-Channel render/parse round-trip
- LACP and static aggregation semantic round-trip
- device intent -> resolver -> LAG renderer path
- aggregate member speed mismatch rejection

## Production build note
The full `prepare:launch` result must be judged separately from these feature gates. If the temporary ZIP environment does not contain a complete `node_modules`, run `npm ci` before the full production gate.

## Core regression result
PASS:
- Device Platform Catalog
- Interface/VLAN five-vendor round trip
- Platform Profiles
- Shared Vendor Renderer
- Tool Registry / Workflow Registry / Tool Navigation
- Architecture: 21 active tools / 56 public pages / 54 sitemap URLs / 0 errors / 0 warnings
- Engineering Rules
- ACL core and ACL rules
- Phase1 acceptance
- Phase2 Foundation / Domain2 / Platform Profile / Device Catalog acceptance
- Topology + LAG acceptance

## Full production gate result
`npm run prepare:launch` was started. Registry/config/rules generation passed, then `build-multilingual.js` stopped before application regression because the temporary extracted environment does not contain the `acorn` dependency. `npm ci --ignore-scripts --no-audit --no-fund` was attempted but the container dependency install timed out.

This is recorded as an environment dependency limitation, not a topology/LAG feature-test failure. In a normal project environment run:

```bash
npm ci
npm run prepare:launch
```
