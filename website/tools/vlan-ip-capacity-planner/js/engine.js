(function(r,f){const a=f();if(typeof module==='object'&&module.exports)module.exports=a;r.NELToolEngine=a;})(typeof globalThis!=='undefined'?globalThis:this,function(){'use strict';function calculate(i){if(!i)return{ok:false};const users=+i.users,perUser=+i.perUser,infra=+i.infra,growth=+i.growth/100,maxHosts=+i.maxHosts,vlanStart=+i.vlanStart;if(![users,perUser,infra,growth,maxHosts,vlanStart].every(Number.isFinite)||!(users>=0&&perUser>=0&&infra>=0&&growth>=0&&maxHosts>=2&&maxHosts<=65534&&vlanStart>=1&&vlanStart<=4094))return{ok:false};const plannedHosts=Math.ceil((users*(1+perUser)+infra)*(1+growth));const vlanCount=Math.max(1,Math.ceil(plannedHosts/maxHosts));const required=Math.max(2,Math.ceil(plannedHosts/vlanCount));let hostBits=2;while(Math.pow(2,hostBits)-2<required&&hostBits<30)hostBits++;const prefix=32-hostBits,usablePerVlan=Math.pow(2,hostBits)-2,totalUsable=usablePerVlan*vlanCount,headroom=totalUsable-plannedHosts,utilization=plannedHosts/totalUsable*100,end=vlanStart+vlanCount-1;if(end>4094)return{ok:false};return{ok:true,plannedHosts,vlanCount,prefix:'/'+prefix,usablePerVlan,totalUsable,headroom,utilization:+utilization.toFixed(1),vlanRange:vlanStart+(end===vlanStart?'':'–'+end),status:utilization<=80?'pass':utilization<=90?'warning':'fail'};}return{calculate};});
