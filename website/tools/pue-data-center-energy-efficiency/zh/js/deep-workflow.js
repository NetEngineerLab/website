// The localized page loads the shared locale-aware workflow from the parent tool directory.
(() => { const script = document.createElement('script'); script.src = '../js/deep-workflow.js'; document.head.appendChild(script); })();
