(function(root,factory){const api=factory();if(typeof module==="object"&&module.exports)module.exports=api;root.NELCoolingSizingEngine=api;})(typeof globalThis!=="undefined"?globalThis:this,function(){"use strict";const f=(v,d=0)=>Number.isFinite(Number(v))?Number(v):d,cl=(v,a,b)=>Math.max(a,Math.min(b,v));function calculate(i={}){const errors=[],warnings=[];const it=f(i.itKW),upsLoss=f(i.upsLossKW),pduLoss=f(i.pduLossKW),lighting=f(i.lightingKW),people=Math.max(0,Math.floor(f(i.people))),personW=f(i.wPerPerson,120),envelope=f(i.envelopeKW),other=f(i.otherKW),head=cl(f(i.headroomPct,15),0,100)/100,unitKW=f(i.unitSensibleKW),redundancy=Math.max(0,Math.floor(f(i.redundancyUnits,1))),installed=Math.max(0,Math.floor(f(i.installedUnits))),deltaT=f(i.deltaTC,10),rho=1.2,cp=1.006;if(!(it>=0&&unitKW>0&&deltaT>0))errors.push('input');if(errors.length)return{ok:false,errors,warnings};const peopleKW=people*personW/1000,totalBaseKW=it+upsLoss+pduLoss+lighting+peopleKW+envelope+other,designKW=totalBaseKW*(1+head),activeUnits=Math.ceil(designKW/unitKW),recommendedUnits=activeUnits+redundancy,selected=installed||recommendedUnits,normalCapacityKW=selected*unitKW,after1KW=Math.max(0,(selected-1)*unitKW),survives1=after1KW>=designKW,utilPct=normalCapacityKW?designKW/normalCapacityKW*100:0;if(!survives1)warnings.push('n1Fail');if(utilPct>90)warnings.push('highUtil');if(utilPct<25)warnings.push('lowUtil');const btuH=designKW*3412.142,tons=designKW/3.5168525,airflowM3h=designKW*3600/(rho*cp*deltaT),nonItKW=totalBaseKW-it,itSharePct=totalBaseKW?it/totalBaseKW*100:0;return{ok:true,warnings,peopleKW,totalBaseKW,nonItKW,designKW,activeUnits,recommendedUnits,selectedUnits:selected,normalCapacityKW,after1KW,survives1,utilPct,btuH,tons,airflowM3h,itSharePct};}return{calculate};});