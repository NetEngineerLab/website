"use strict";const M=require("../model");
function parse(input){const lines=String(input).split(/\r?\n/).map(x=>x.trim()).filter(Boolean);let name,description=null,mode,nativeVlan=null;const members=[];for(const s of lines){let m;if(m=s.match(/^set interfaces (\S+) description \"?(.*?)\"?$/i)){name=m[1];description=m[2].replace(/\"$/,'')}else if(m=s.match(/^set interfaces (\S+) unit 0 family ethernet-switching interface-mode (access|trunk)$/i)){name=name||m[1];mode=m[2]}else if(m=s.match(/^set interfaces (\S+) unit 0 family ethernet-switching vlan members (\d+)$/i)){name=name||m[1];members.push(Number(m[2]))}else if(m=s.match(/^set interfaces (\S+) native-vlan-id (\d+)$/i)){name=name||m[1];nativeVlan=Number(m[2])}}const canonical=M.create(mode==="access"?{name,description,mode,accessVlan:members[0]}:{name,description,mode,allowedVlans:members,nativeVlan});return {model:canonical,ir:canonical}}
module.exports={parse};
